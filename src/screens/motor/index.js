import { Text } from "native-base";
import Layout from "../../components/layout";
import { useEffect } from "react";

export default function MotorScreen({ navigation }) {
  return (
    <Layout>
      <Text>Motor Screen</Text>
    </Layout>
  )
}